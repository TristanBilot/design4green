import { visibleItems as visibleItemsType } from '../types';
interface Props {
    cb: () => void;
    condition: boolean;
    visibleItems: visibleItemsType;
}
declare function useOnUpdate({ cb, condition, visibleItems, }: Props): void;
export default useOnUpdate;
